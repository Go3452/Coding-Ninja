public class Solution {

    public static long transactions(int[] money) {
        long sum = 0,max=0;
        int n=money.length;
        for(int i=0;i<n;i++)
        {
         sum+=money[i];
         if(sum>max)
          max=sum;
        } 
        return max;
    }
}
