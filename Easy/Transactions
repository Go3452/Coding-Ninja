public class Solution {

    public static long transactions(int[] money) {
         long ans=0,sum=0;
         for(int i=0;i<money.length;i++)
         {
          sum+=money[i];
          if(sum>ans)
           ans=sum;
         }
         return ans;
    }
}
