public class CheckRotation{	
	//arr is the given sorted array in which you need to find the rotated index
	public static int arrayRotateCheck(int[] arr){
		for(int i=0;i<arr.length-1;i++)
		{
		 if(arr[i]>arr[i+1])
		  return i+1;
		}
		return 0;
	}
}
